<resources>

    <!-- 底部弹出的Dialog或者PopupWindow的show和dismiss动画 -->
    <style name="animation_style_dialog_bottom" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/anim_bottom_to_top</item>
        <item name="android:windowExitAnimation">@anim/anim_top_to_bottom</item>
    </style>

    <!-- LoadingDialog样式 -->
    <style name="dialog_style_loading" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <!-- 是否对话框背景变暗 -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- CustomDialog样式 -->
    <style name="dialog_style_custom" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- 底部弹出的Dialog样式 -->
    <style name="dialog_style_bottom" parent="android:Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/animation_style_dialog_bottom</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
